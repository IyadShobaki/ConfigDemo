@page "/"
@using Microsoft.Extensions.Configuration
@inject IConfiguration config

<h1>Hello, world!</h1>

Welcome to your new app.

@code{      
    protected override void OnInitialized()
    {
        base.OnInitialized();
        int val = config.GetValue<int>("WeatherForecast:ForecastDays");
    }
}
